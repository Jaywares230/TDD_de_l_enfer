<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Club THP', root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Home', root_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to 'membres du club', index_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
            <ul class="nav navbar-nav">
              
              <li><%= link_to 'Deconnexion', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'connecte toi', new_user_session_path %></li>
          <li><%= link_to 'inscris toi ', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>